package org.first.team342.vision.processing;

import org.first.team342.vision.Image;

/**
 *
 * @author FIRST Team 342
 */
public interface ImageProcessor {
    public Image processImage(Image image);
}
